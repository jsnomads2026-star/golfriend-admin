import test from 'node:test';import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import{resolve}from'node:path';import{loadWorld,resetWorld,seedWorld,verifyWorld}from'./small-business-monday-world.mjs';
test('shared Monday world is deterministic and production-write free',()=>{const a=verifyWorld(),b=verifyWorld();assert.equal(a.ok,true);assert.equal(a.digest,b.digest);assert.equal(seedWorld().productionWrites,false)});
test('reset returns an independent canonical snapshot',()=>{const original=loadWorld(),reset=resetWorld();reset.businesses[0].status='closed';assert.equal(original.businesses[0].status,'active')});
test('hostile identities and required lifecycle scenarios exist',()=>{const world=loadWorld();assert(world.identities.some(value=>value.role==='none'));for(const scenario of ['canonical_id_replacement_denied','stale_concurrent_correction','offline_edit_retry','double_submission','dnc_frequency_enforcement','jhcc_prepare_no_transmission'])assert(world.scenarios.includes(scenario))});
test('mobile contract is exact non-live and Lounge-bound',()=>{const contract=JSON.parse(readFileSync(resolve('docs/SMALL-BUSINESS-MOBILE-INTEGRATION-v1.json'),'utf8'));assert.equal(contract.schema,'golfriend.small-business.mobile-integration.v1');assert.deepEqual(contract.navigation,{origin:'/lounge',back:'/lounge',exit:'/lounge',acceptanceSideEffects:false});assert.equal(contract.authority.auth,true);assert.equal(contract.authority.appCheck,true);assert.equal(contract.privacy.forbidden.includes('rawCoordinates'),true);assert.equal(contract.proactiveCard.externalHandoffRequires.length,2)});
