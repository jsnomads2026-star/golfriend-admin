import{spawnSync}from'node:child_process';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),env={...process.env,FIREBASE_EMULATORS_PATH:path.join(root,'.firebase','emulators')},runner=process.platform==='win32'?'npx.cmd':'npx',command=`${runner} --yes firebase-tools@15.1.0 emulators:exec --config firebase.marketing-emulator.json --project demo-marketing-assets --only firestore,storage "node scripts/marketing-rules-emulator.test.mjs"`,result=spawnSync(command,{cwd:root,env,stdio:'inherit',shell:true});if(result.error)console.error(result.error);process.exit(result.status??1);
