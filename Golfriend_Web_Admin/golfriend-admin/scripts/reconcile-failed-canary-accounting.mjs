#!/usr/bin/env node
import {createRequire} from 'node:module';
const require=createRequire(import.meta.url),d=require('../functions-course-catalogue/domain.js'),PROJECT='golfriend-v2',ROOT=`https://firestore.googleapis.com/v1/projects/${PROJECT}/databases/(default)/documents`,SOURCE_ID=process.argv.find(value=>value.startsWith('--receipt='))?.split('=')[1];
if(!SOURCE_ID)throw Error('SOURCE_CANARY_RECEIPT_REQUIRED');
const auth=require(process.env.FIREBASE_TOOLS_AUTH_MODULE||'C:/Users/Windows/AppData/Roaming/npm/node_modules/firebase-tools/lib/auth'),account=auth.getGlobalDefaultAccount(),scopes=Array.isArray(account.tokens.scopes)?account.tokens.scopes:String(account.tokens.scope||'').split(/\s+/).filter(Boolean),credential=await auth.getAccessToken(account.tokens.refresh_token,scopes),access=credential.access_token;
const encode=value=>value===null?{nullValue:null}:typeof value==='string'?{stringValue:value}:typeof value==='boolean'?{booleanValue:value}:typeof value==='number'?(Number.isInteger(value)?{integerValue:String(value)}:{doubleValue:value}):Array.isArray(value)?{arrayValue:{values:value.map(encode)}}:{mapValue:{fields:Object.fromEntries(Object.entries(value).map(([key,item])=>[key,encode(item)]))}};
const decode=value=>value?.stringValue??value?.booleanValue??value?.timestampValue??(value?.integerValue!==undefined?Number(value.integerValue):value?.doubleValue??(value?.nullValue===null?null:value?.mapValue?Object.fromEntries(Object.entries(value.mapValue.fields||{}).map(([key,item])=>[key,decode(item)])):value?.arrayValue?(value.arrayValue.values||[]).map(decode):undefined));
async function google(url,init={}){const response=await fetch(url,{...init,headers:{Authorization:`Bearer ${access}`,'Content-Type':'application/json',...(init.headers||{})}}),body=await response.text();if(!response.ok)throw Error(`GOOGLE_${response.status}:${body.slice(0,160)}`);return body?JSON.parse(body):{};}
async function get(collection,id){const document=await google(`${ROOT}/${collection}/${id}`);return Object.fromEntries(Object.entries(document.fields||{}).map(([key,item])=>[key,decode(item)]));}
const source=await get('golf_api_canary_receipts',SOURCE_ID),assessment=d.assessCanaryAccountingEvidence(source);if(assessment.eligible)throw Error('EVIDENCE_ELIGIBLE_REQUIRES_SERVER_TRANSACTIONAL_RECONCILIATION');const quota=await get('golf_api_quota',new Date().toISOString().slice(0,7)),ledger={configuredBudget:Number(quota.configuredBudget||0),emergencyReserve:Number(quota.emergencyReserve||0),weightedCompleted:Number(quota.weightedCompleted||0),weightedFailed:Number(quota.weightedFailed||0),weightedReserved:Number(quota.weightedReserved||0),weightedReleased:Number(quota.weightedReleased||0),providerReportedRemaining:Number.isFinite(Number(quota.providerReportedRemaining))?Number(quota.providerReportedRemaining):null,providerEvidenceAt:quota.providerEvidenceAt??null},createdAt=new Date().toISOString(),payload={schema:'golfriend.golf-api-canary-reconciliation-receipt.v1',projectId:PROJECT,sourceCanaryReceiptId:SOURCE_ID,sourceCanaryDigest:source.digest,disposition:'blocked_insufficient_immutable_evidence',reasons:assessment.reasons,requestedWeight:source.weightedAmount??null,ledgerBefore:ledger,ledgerAfter:ledger,mutationApplied:false,providerRemainingDisposition:'UNKNOWN_OR_STALE_RETAINED',providerRequests:0,createdAt},digest=d.digest(payload),receiptId=`canary-reconciliation-${d.digest(SOURCE_ID).slice(0,12)}-${digest.slice(0,12)}`;
await google(`${ROOT}/golf_api_canary_reconciliation_receipts?documentId=${encodeURIComponent(receiptId)}`,{method:'POST',body:JSON.stringify({fields:Object.fromEntries(Object.entries({...payload,receiptId,digest,immutable:true}).map(([key,value])=>[key,encode(value)]))})});
console.log(JSON.stringify({receiptId,digest,disposition:payload.disposition,reasons:payload.reasons,mutationApplied:false,ledgerBefore:ledger,ledgerAfter:ledger,providerRequests:0,secretValueAccessed:false}));
