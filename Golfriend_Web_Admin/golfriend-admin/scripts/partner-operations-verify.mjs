import assert from'node:assert/strict';import fs from'node:fs';import{decisionPreview,filterPartnerRequests,onboardingChecklist,PARTNER_LOCALES,PARTNER_STATUSES,PARTNER_TYPES,partnerSummary,normalizePartnerRequest}from'../src/components/admin/v2/partnerOperationsModel.mjs';assert.deepEqual(PARTNER_LOCALES,['en','th','ko','ja','zh','es','fr','de']);assert.equal(PARTNER_TYPES.length,6);assert.equal(PARTNER_STATUSES.length,7);const rows=[normalizePartnerRequest({id:'1',organization:'Alpha',type:'golf_course',contactLocale:'th',submittedAt:'2026-01-02',status:'new',evidence:{identity:true}}),normalizePartnerRequest({id:'2',organization:'Beta',status:'source_unavailable'})];assert.equal(partnerSummary(rows).total,2);assert.deepEqual(filterPartnerRequests(rows,{query:'alpha',type:'golf_course',status:'new',locale:'th'}).map(x=>x.id),['1']);assert.equal(onboardingChecklist(rows[0]).filter(x=>x.complete).length,1);assert.equal(decisionPreview(rows[0],'approve','en').submissionAvailable,false);assert.throws(()=>decisionPreview(rows[0],'send','en'));const ui=fs.readFileSync(new URL('../src/components/admin/v2/V2PartnerOperations.tsx',import.meta.url),'utf8'),provider=fs.readFileSync(new URL('../src/components/admin/v2/partnerOperationsProvider.ts',import.meta.url),'utf8'),app=fs.readFileSync(new URL('../src/App.tsx',import.meta.url),'utf8'),css=fs.readFileSync(new URL('../src/components/admin/v2/V2PartnerOperations.css',import.meta.url),'utf8');assert.match(app,/activeArea === 'partners' && <V2PartnerOperations/);assert.match(provider,/decisionService:null/);assert.match(provider,/source:'local-preview'/);assert.doesNotMatch(ui+provider,/firebase|firestore|addDoc|setDoc|updateDoc|deleteDoc|httpsCallable/i);assert.match(ui,/Preview decision/);assert.match(ui,/Submit decision unavailable/);assert.match(ui,/navigator\.clipboard\.writeText/);assert.match(ui,/None verified/);assert.doesNotMatch(ui+provider,/email sent|account created|partner accepted|notification sent/i);assert.match(css,/@media\(max-width:700px\)/);console.log('Partner operations verification PASS: route, exact eight locales, six types, seven states, filtering/sorting, checklist honesty, preview-before-decision, copy/export, no-write and responsive boundaries.');
