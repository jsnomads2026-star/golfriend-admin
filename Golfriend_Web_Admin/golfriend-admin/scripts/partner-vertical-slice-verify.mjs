import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';const root=new URL('../',import.meta.url).pathname.slice(1);const html=readFileSync(join(root,'public/partner-review.html'),'utf8');const js=readFileSync(join(root,'public/partner-review.js'),'utf8');const css=readFileSync(join(root,'public/partner-review.css'),'utf8');assert.match(html,/NON-PRODUCTION/);assert.match(html,/type="password"/);assert.match(html,/partner-review\.js/);assert.match(js,/\/api\/admin\/sessions/);assert.match(js,/information-requests/);assert.match(js,/bookings/);assert.match(js,/X-CSRF-Token/);assert.doesNotMatch(js,/firebase|production URL|Bearer /i);assert.match(css,/min-height:44px/);console.log('partner vertical slice admin verify: passed');
