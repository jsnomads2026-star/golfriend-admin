import assert from'node:assert/strict';import{readFileSync}from'node:fs';const files=['functions/src/enterpriseCourseIntakeSecurity.ts','src/components/B2B/enterprise/StaffRoles.tsx','src/components/B2B/enterprise/staffRemovalCopy.ts'];let n=0;for(const f of files){assert.doesNotMatch(readFileSync(new URL(`../${f}`,import.meta.url),'utf8'),/(-----BEGIN [A-Z ]+PRIVATE KEY-----|AIza[0-9A-Za-z_-]{20,}|sk_live_[0-9A-Za-z]+)/);n++}console.log(`Course Intake secret scan PASS: ${n}/${n}`);
