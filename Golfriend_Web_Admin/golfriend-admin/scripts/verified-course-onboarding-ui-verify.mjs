import assert from"node:assert/strict";import{readFileSync}from"node:fs";const r=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),ui=r("src/components/B2B/VerifiedCourseOnboarding.tsx"),svc=r("src/components/B2B/verifiedCourseOnboardingService.ts"),copy=r("src/i18n/partner/enterpriseCourseOnboarding.ts"),dashboard=r("src/components/B2B/EnterpriseDashboard.tsx"),journey=r("src/components/B2B/PartnerApplicationJourney.tsx"),runtime=r("functions/src/enterpriseOnboardingRuntime.ts");let n=0;const t=(x,f)=>{f();console.log(`ok ${++n} - ${x}`)};
t("authorized Enterprise mount",()=>{assert.match(dashboard,/import VerifiedCourseOnboarding/);assert.match(dashboard,/activeTab === 'venues' && <><VerifiedCourseOnboarding/)});t("pre-partner Join entry",()=>{assert.match(journey,/<VerifiedCourseOnboarding \/>/);assert.doesNotMatch(journey,/view\?\.application && <VerifiedCourseOnboarding/)});
const names=["getEnterpriseCourseOnboardingV1","saveEnterpriseCourseOnboardingDraftV1","submitEnterpriseCourseOnboardingV1","previewEnterpriseAgreementV1","acceptEnterpriseAgreementV1","withdrawEnterpriseCourseOnboardingV1"];t("callable parity",()=>names.forEach(x=>{assert.match(svc,new RegExp(x));assert.match(runtime,new RegExp(`export const ${x}`))}));
t("top-level payload parity",()=>{assert.match(svc,/\{context,commandId,expectedVersion:baseVersion,draft:flatten\(draft\)\}/);assert.match(svc,/contentDigest:a\.localizedDigest,explicitlyAccepted:true/)});t("projection adapter",()=>{assert.match(svc,/raw\?\.application/);assert.match(svc,/raw\?\.state/);assert.match(svc,/agreementAcceptance/)});
t("producer draft hydration",()=>{assert.match(svc,/a\?\.draft/);assert.match(ui,/current\.draft\?\{/);assert.match(ui,/setDraft\(hydrated\)/);["legalName","registeredAddress","representativeName","courseProfileAttemptId","dataUseAcknowledged","bookingCommissionAcknowledged"].forEach(x=>assert.match(svc,new RegExp(x)))});
t("server-derived explicit multi-course scope",()=>{assert.match(ui,/organizationAuthorityService\.load/);assert.match(ui,/setScopes\(available\)/);assert.match(ui,/phase===\"scope\"/);assert.doesNotMatch(ui,/available\[0\]/);["membershipId","organizationId","propertyId","courseId"].forEach(x=>assert.match(svc,new RegExp(x)))});
t("persisted fields only",()=>{["representativeAuthorityDeclaration","courseProfileAttemptId","dataUseAcknowledged","memberPurposeAcknowledged","tournamentPurposeAcknowledged","bookingCommissionAcknowledged"].forEach(x=>assert.match(svc,new RegExp(x)));assert.doesNotMatch(ui,/field\("profile","description"/)});
t("exact trials and authorized offer",()=>{assert.match(svc,/TRIAL_OPTIONS=\[30,90\]/);assert.match(ui,/trialOfferId/)});t("agreement binding",()=>["canonicalDigest","localizedDigest","canonicalContent","commissionPolicyRef","trialTermsRef","dataUseTermsRef","explicitlyAccepted:true"].forEach(x=>assert.match(svc,new RegExp(x))));
t("agreement invalidation and fallback",()=>{assert.match(ui,/setAgreement\(null\)/);assert.match(ui,/setAccepted\(false\)/);assert.match(ui,/canonicalContent/);assert.match(ui,/lang="en"/)});t("stable retry commands",()=>assert.match(ui,/commands\.current\[kind\]/));
t("honest Admin lifecycle",()=>{["changes_requested","approved_for_trial","trial_active","trial_expired","conversion_review","active_partner","suspended","withdrawn"].forEach(x=>assert.match(svc,new RegExp(x)));assert.match(copy,/Portal cannot approve/);assert.match(ui,/adminUnavailable/)});t("exact locales",()=>assert.match(copy,/\["en","th","ko","ja","zh","es","fr","de"\]as const/));t("labels localized independently",()=>["en","th","ko","ja","zh","es","fr","de"].forEach(x=>assert.match(copy,new RegExp(`${x}:\\{membershipId:`))));
t("accessible states",()=>[/aria-live=/,/aria-busy=/,/role="alert"/,/aria-current=/,/tabIndex=\{0\}/,/minHeight:48/].forEach(x=>assert.match(ui,x)));t("no datastore or financial authority",()=>assert.doesNotMatch(ui+svc,/firestore|setDoc|addDoc|collection\(|stripe|collectPayment|activateProvider/i));console.log(`enterprise onboarding consumer compatibility: ${n} checks passed.`);
