import assert from'node:assert/strict';import fs from'node:fs';
const fn=fs.readFileSync(new URL('../functions/src/courseIngestion.ts',import.meta.url),'utf8'),core=fs.readFileSync(new URL('../functions/src/courseOperationsProjection.ts',import.meta.url),'utf8'),service=fs.readFileSync(new URL('../src/components/admin/v2/courseOperationsService.ts',import.meta.url),'utf8'),ui=fs.readFileSync(new URL('../src/components/admin/v2/V2CourseOperationsMonitor.tsx',import.meta.url),'utf8'),host=fs.readFileSync(new URL('../src/components/admin/v2/V2CourseOperations.tsx',import.meta.url),'utf8'),workflow=fs.readFileSync(new URL('../../../.github/workflows/e6-admin-verification.yml',import.meta.url),'utf8');
assert.match(fn,/getCourseIngestionOperations = onCall\(\{enforceAppCheck:true\}/);assert.match(fn,/prepareCourseIngestionRetry = onCall\(\{enforceAppCheck:true\}/);assert.match(fn,/requireCoordinator\(request\.auth\.uid\)/);assert.match(fn,/countryBreakdown/);assert.match(fn,/transaction\.create\(retryRef/);assert.doesNotMatch(fn.slice(fn.indexOf('prepareCourseIngestionRetry')),/golfApiGet\(/);
assert.match(core,/estimatedCallsUsed/);assert.match(core,/monthlyLimit/);assert.match(core,/resetPeriod/);assert.match(core,/resetsAt/);assert.match(core,/ratio>=\.95/);assert.match(core,/ratio>=\.8/);assert.match(core,/deterministicRetryJobId/);assert.match(core,/completed_with_errors/);assert.match(core,/recovered/);
assert.match(service,/getCourseIngestionOperations/);assert.match(service,/prepareCourseIngestionRetry/);assert.match(ui,/service\.prepareFailedRetry/);assert.match(ui,/service\.commitRegion\(retryJob\)/);assert.match(ui,/role="alert"/);assert.match(ui,/role="status"/);for(const locale of ['en','th','ko','ja','zh','es','fr','de'])assert.match(ui,new RegExp(`${locale}:`));assert.match(host,/V2CourseOperationsMonitor service=\{service\}/);
for(const gate of ['npm test','verify:e6-course-growth','verify:e6-ingestion-admin','verify:e6-operations','verify:course-operations','npm run build','git diff --check'])assert.match(workflow,new RegExp(gate.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));
console.log('E6 operations PASS: authoritative country growth, server quota projection, idempotent failed-job retry, mounted eight-locale UI, and GitHub Actions gates.');
