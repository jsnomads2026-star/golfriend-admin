import assert from'node:assert/strict';import{readFileSync}from'node:fs';const s=readFileSync(new URL('../src/components/B2B/enterprise/staffRemovalCopy.ts',import.meta.url),'utf8');let n=0;for(const locale of ['en','th','ko','ja','zh','es','fr','de']){assert.match(s,new RegExp(`\\n\\s*${locale}:\\{`));n++}for(const key of ['remove','confirm','removed','replayed','failed','reason']){assert.match(s,new RegExp(`${key}:`));n++}assert.doesNotMatch(s,/\.\.\.en/);n++;console.log(`Course Intake locales PASS: ${n}/${n}`);
