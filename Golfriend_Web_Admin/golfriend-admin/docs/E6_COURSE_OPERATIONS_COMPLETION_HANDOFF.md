# E6 Course Operations Completion Handoff

Inventory promotion authority: commit `be1e2ca21cd3b26e6f7b8ef5a2b1f43705c158ba` on `codex/golf-api-course-sync`, draft PR #13.

Evidence boundary: source, unit/contract tests, production build, and GitHub Actions are complete. Production Firebase deployment, provider credentials, live quota use, and production course growth are not commissioned and must not be inferred from this handoff.

| Atomic function | Construction status | Producer / authority | Runtime contract | Mounted Admin consumer | Focused tests | Eight-locale and accessibility evidence | CI evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Course database growth by country | CODE_COMPLETE | `getCourseIngestionOperations` reads server Firebase `courses` plus immutable `course_sync_receipts`; `projectCountryGrowth` produces current total, added, updated, failed, and last successful ingestion per authoritative country, with `ZZ` only for missing/invalid values. | Firebase Functions v2 callable, App Check enforced, authenticated active staff required, schema `golfriend.course-operations/v1`. | `V2CourseOperationsMonitor`, mounted by `V2CourseOperations`, renders the country table with loading, empty, and error states. | `courseOperationsProjection.test.ts`: country preservation, `ZZ`, current totals, receipt aggregation, latest-success semantics. `verify:e6-operations`: callable, projection, service, mount, and state contract. | Copy map contains `en`, `th`, `ko`, `ja`, `zh`, `es`, `fr`, `de`; semantic table, `role=status`, and `role=alert`. | `E6 Admin verification` runs Functions, E6 verifiers, Course Operations regression, build, and whitespace. PR #13 push and pull-request runs completed SUCCESS in 42 seconds on 2026-08-14. |
| Authoritative monthly Golf API quota display | CODE_COMPLETE | `getCourseIngestionOperations` reads only server document `platform/golfApiUsage`; `projectQuota` requires `monthlyLimit`, server ledger `estimatedCallsUsed`, `resetPeriod`, and `resetsAt`, then calculates remaining and 80%/95% warning states. Missing authority returns `unconfigured`, never a client estimate. | Same authenticated/App Check callable and schema; source disclosed as `server_quota_ledger`. | `V2CourseOperationsMonitor` renders used, remaining, limit, reset period/time, and textual warning level; unavailable is explicit. | `courseOperationsProjection.test.ts`: unconfigured fail-closed, ledger projection, warning and critical thresholds. `verify:e6-operations`: server-only fields and mounted display contract. | Same eight locale map; quota unavailable uses a status region and warning level is visible text rather than color alone. | Same two successful PR #13 `E6 Admin verification` runs. |
| Controlled failed-job retry | CODE_COMPLETE | `prepareCourseIngestionRetry` accepts only a server job ID, requires `completed_with_errors` or `recovered`, excludes already-created courses, derives deterministic retry ID, and transactionally creates one preview job. It never calls Golf API. `commitCourseRegionImport` performs confirmed execution using the existing lease owner/token/expiry, quota reservation, idempotency, and recovery contracts. | Preparation and commit are Firebase Functions v2 callables with App Check and active-staff authorization. Retry schema is `golfriend.course-ingestion-retry/v1`; repeated preparation replays the deterministic job. | `V2CourseOperationsMonitor` provides prepare, confirmation, executing, result counts, and error states, then refreshes authoritative projection. | `courseOperationsProjection.test.ts`: retry eligibility, deterministic IDs, existing-course exclusion and ordering. Recovery tests: 14/14. `verify:e6-operations`: no provider call in preparation, transaction create, service handoff, and mounted states. | Same eight locale map; controls are native buttons, confirmation is an accessible group, progress/result use status, and failures use alert. | Same two successful PR #13 `E6 Admin verification` runs. |

Validation totals at handoff: Functions `120/120`; E6 course-growth, Admin ingestion, E6 operations, and Course Operations verifiers PASS; TypeScript/Vite production build PASS with 117 modules; `git diff --check` PASS.

Production commissioning still required for all three rows: deploy approved Functions and rules, configure authoritative quota/reset data and recovery policy, configure provider credentials through approved secret management, execute authorized non-destructive production smoke tests, observe immutable receipts and country/quota projections, and obtain operational approval. Keep status `CODE_COMPLETE / COMMISSIONING_REQUIRED` until that evidence exists.
