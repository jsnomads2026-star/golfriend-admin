/* eslint-disable @typescript-eslint/no-explicit-any */
import{useEffect,useState}from'react';import{generateReport,periodRange,REPORT_LOCALES,reportToCsv,reportToJson,reportToTxt}from'./reportingModel.mjs';import{defaultReportTransmitter,localReportingProvider,type ReportingSourceProvider,type ReportTransmitter}from'./reportingProvider';import'./V2AdminReports.css';const label=(v:string)=>v.replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase());
const COPY=Object.fromEntries(REPORT_LOCALES.map((locale:string)=>[locale,{title:'Reports to JHCC',warning:'Automatic JHCC delivery awaiting approved reporting contract',period:'Reporting period',generate:'Generate local report',copy:'Copy executive summary',txt:'Export .txt',csv:'Export .csv',json:'Export .json',transmit:'Transmission unavailable',summary:'Executive summary'}]));
export default function V2AdminReports({provider=localReportingProvider,transmitter=defaultReportTransmitter}:{provider?:ReportingSourceProvider;transmitter?:ReportTransmitter|null}){const[sources,setSources]=useState<any>(null),[error,setError]=useState(false),[kind,setKind]=useState('7d'),[start,setStart]=useState(''),[end,setEnd]=useState(''),[locale,setLocale]=useState('en'),[report,setReport]=useState<any>(null),[rangeError,setRangeError]=useState(false),[exportReady,setExportReady]=useState(false),[transmitPreview,setTransmitPreview]=useState(false);useEffect(()=>{void provider.load().then(setSources,()=>setError(true))},[provider]);const c=COPY[locale];if(error)return <div className="reports-state" role="alert">Source unavailable. <button onClick={()=>location.reload()}>Retry</button></div>;if(!sources)return <div className="reports-state" role="status">Loading reporting sources…</div>;const generate=()=>{try{const period=periodRange(kind,new Date(),{start,end});setReport(generateReport({period,sources,generatedAt:new Date().toISOString()}));setRangeError(false);setExportReady(true)}catch{setRangeError(true);setReport(null)}};const download=(text:string,type:string,name:string)=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();URL.revokeObjectURL(a.href)};const copy=async()=>{if(report)await navigator.clipboard.writeText(report.executiveSummary.highlights.concat(report.executiveSummary.risks,report.executiveSummary.attention,report.executiveSummary.nextActions).map((x:any)=>`[${x.sectionId}] ${x.text}`).join('\n'))};return <div className="reports-ops"><header><div><span>GOLFRIEND OPERATIONS · LOCAL PREPARATION</span><h2>{c.title}</h2><p>Prepare traceable Golfriend reports without building or changing JHCC.</p></div><aside><b>{c.warning}</b><small>No credentials, endpoint, schedule, upload, or delivery is configured.</small></aside></header><section className="report-controls"><label>Locale<select value={locale} onChange={e=>setLocale(e.target.value)}>{REPORT_LOCALES.map((x:string)=><option key={x}>{x}</option>)}</select></label><label>{c.period}<select value={kind} onChange={e=>setKind(e.target.value)}><option value="today">Today</option><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option><option value="custom">Custom</option></select></label>{kind==='custom'&&<><input aria-label="Custom start" type="date" value={start} onChange={e=>setStart(e.target.value)}/><input aria-label="Custom end" type="date" value={end} onChange={e=>setEnd(e.target.value)}/></>}<button onClick={generate}>{c.generate}</button></section>{rangeError&&<div className="reports-state is-error">Invalid custom range. Correct the dates and retry.</div>}{report&&<><section className="report-overview"><article><span>Latest prepared report</span><b>{report.generatedAt}</b></article><article><span>Period</span><b>{report.period.start.slice(0,10)} — {report.period.end.slice(0,10)}</b></article>{Object.entries(report.coverage).map(([k,v])=><article key={k}><span>{label(k)}</span><b>{String(v)}</b></article>)}</section><section className="report-grid"><main>{report.sections.map((s:any)=><article key={s.id} className={`is-${s.status}`}><span>{label(s.id)}</span><b>{label(s.status)}</b><p>{s.sourceLabel}</p><small>{s.sourceKind==='local-preview'?'Preview only · excluded from production totals':s.sourceKind}</small></article>)}</main><aside><span>{c.summary}</span><h3>Traceable highlights and blockers</h3>{report.executiveSummary.highlights.concat(report.executiveSummary.risks,report.executiveSummary.attention,report.executiveSummary.nextActions).map((x:any,i:number)=><p key={`${x.sectionId}-${i}`}><b>{label(x.sectionId)}</b> {x.text}</p>)}<button onClick={()=>void copy()}>{c.copy}</button></aside></section><section className="report-actions"><button onClick={()=>download(reportToTxt(report),'text/plain','golfriend-report.txt')}>{c.txt}</button><button onClick={()=>download(reportToCsv(report),'text/csv','golfriend-report.csv')}>{c.csv}</button><button onClick={()=>download(reportToJson(report),'application/json','golfriend-report.json')}>{c.json}</button><button onClick={()=>setTransmitPreview(true)}>Preview future payload</button><button disabled={!transmitter}>{c.transmit}</button></section>{exportReady&&<p className="export-ready">Export ready · schema {report.schema} · version {report.version}</p>}{transmitPreview&&<div className="transmission"><b>Future transmission preview</b><pre>{JSON.stringify({schema:report.schema,generatedAt:report.generatedAt,period:report.period,coverage:report.coverage},null,2)}</pre><p>{c.warning}</p><button disabled>Confirm and send unavailable</button></div>}</>}</div>}
