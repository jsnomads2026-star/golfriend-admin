import { ADMIN_AREAS, type AdminArea } from './adminNavigation';
export default function V2AdminOverview({ onOpen }: { onOpen: (area: AdminArea) => void }) {
  return <div className="v2-admin-overview"><section className="v2-admin-hero"><div><span>GOLFRIEND OPERATIONS</span><h2>Keep every round connected.</h2></div><p>One operational view for courses, bookings, partners, publishing and accountable reporting. Golfriend manages its own services here and reports upward to JHCC.</p></section><section className="v2-admin-principles" aria-label="Operating boundaries"><article><span>AUTHORITY</span><strong>Server-owned actions</strong><p>Existing authorization and service boundaries remain the source of truth.</p></article><article><span>COURSE DATA</span><strong>Protected sync</strong><p>Preview, trusted records and manual GPS safeguards remain intact.</p></article><article><span>TEE TIMES</span><strong>Communication, not settlement</strong><p>Golfriend coordinates booking operations; it does not process tee-time payments.</p></article></section><section className="v2-admin-area-grid" aria-label="Operational areas">{ADMIN_AREAS.slice(1).map((area, index) => <button type="button" key={area.id} onClick={() => onOpen(area.id)}><span>{String(index + 2).padStart(2, '0')}</span><strong>{area.label}</strong><i>↗</i></button>)}</section></div>;
}
