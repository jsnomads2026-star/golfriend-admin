export const PARTNER_LOCALES=Object.freeze(['en','th','ko','ja','zh','es','fr','de']);
export const PARTNER_TYPES=Object.freeze(['golf_course','golf_service','sponsor_advertiser','community_media','strategic_partner','unknown']);
export const PARTNER_STATUSES=Object.freeze(['new','under_review','information_required','approved','declined','withdrawn','source_unavailable']);
export function normalizePartnerRequest(row){return{id:String(row.id),organization:String(row.organization||'Unverified organization'),type:PARTNER_TYPES.includes(row.type)?row.type:'unknown',country:String(row.country||'Unverified'),region:String(row.region||'Unverified'),contactName:String(row.contactName||'Withheld / unverified'),contactLocale:PARTNER_LOCALES.includes(row.contactLocale)?row.contactLocale:null,submittedAt:row.submittedAt||null,status:PARTNER_STATUSES.includes(row.status)?row.status:'source_unavailable',owner:row.owner?String(row.owner):null,source:String(row.source||'Source unavailable'),collaboration:String(row.collaboration||'Not provided'),courseIds:Array.isArray(row.courseIds)?row.courseIds.map(String):[],links:Array.isArray(row.links)?row.links.filter((x)=>x?.trusted===true):[],history:Array.isArray(row.history)?row.history:[],evidence:row.evidence&&typeof row.evidence==='object'?row.evidence:{}}}
export function filterPartnerRequests(rows,{query='',type='all',status='all',locale='all',sort='newest'}={}){const n=query.trim().toLowerCase();return rows.filter(r=>(!n||[r.organization,r.country,r.region,r.contactName].some(v=>v.toLowerCase().includes(n)))&&(type==='all'||r.type===type)&&(status==='all'||r.status===status)&&(locale==='all'||r.contactLocale===locale)).sort((a,b)=>sort==='organization'?a.organization.localeCompare(b.organization):String(b.submittedAt||'').localeCompare(String(a.submittedAt||'')));}
export function partnerSummary(rows){return{total:rows.length,byStatus:Object.fromEntries(PARTNER_STATUSES.map(s=>[s,rows.filter(r=>r.status===s).length])),recent:[...rows].filter(r=>r.submittedAt).sort((a,b)=>String(b.submittedAt).localeCompare(String(a.submittedAt))).slice(0,4)}}
export function onboardingChecklist(row){const e=row.evidence||{};return[['identity','Identity/contact verification'],['organization','Organization verification'],['courseReview','Course/profile-data review'],['bookingBoundary','Booking/payment-boundary acknowledgement'],['locale','Preferred communication locale'],['terms','Terms/privacy acknowledgement']].map(([key,label])=>({key,label,complete:e[key]===true}));}
export function decisionPreview(row,decision,locale){if(!row||!['approve','decline','request_information'].includes(decision)||!PARTNER_LOCALES.includes(locale))throw new Error('Decision preview failed validation.');return{requestId:row.id,decision,locale,organization:row.organization,submissionAvailable:false,notice:'Preview only. No update, account, email, or notification has been created.'};}
