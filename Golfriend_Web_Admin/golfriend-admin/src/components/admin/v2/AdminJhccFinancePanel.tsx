import{useState}from'react';import{httpsCallable}from'firebase/functions';import{functions}from'../../../firebaseConfig';import{useAdminLocale}from'./AdminLocaleContext';
const copy:any={en:['JHCC Finance report','Prepare authoritative report','I confirm this exact report','Transmit','Production delivery remains unavailable until commissioned.'],th:['รายงานการเงิน JHCC','เตรียมรายงานที่ได้รับอนุญาต','ฉันยืนยันรายงานฉบับนี้','ส่งรายงาน','การส่งจริงยังไม่พร้อมจนกว่าจะได้รับอนุมัติ'],ko:['JHCC 재무 보고서','권한 있는 보고서 준비','이 정확한 보고서를 확인합니다','전송','승인 전에는 운영 전송을 사용할 수 없습니다.'],ja:['JHCC 財務レポート','権限済みレポートを準備','この正確なレポートを確認します','送信','承認されるまで本番配信は利用できません。'],zh:['JHCC 财务报告','准备授权报告','我确认此准确报告','传输','正式交付在获批前不可用。'],es:['Informe financiero JHCC','Preparar informe autorizado','Confirmo este informe exacto','Transmitir','La entrega de producción no está disponible hasta su aprobación.'],fr:['Rapport financier JHCC','Préparer le rapport autorisé','Je confirme ce rapport exact','Transmettre','La livraison en production reste indisponible avant homologation.'],de:['JHCC-Finanzbericht','Autorisierten Bericht vorbereiten','Ich bestätige diesen exakten Bericht','Übertragen','Die Produktionsübertragung bleibt bis zur Freigabe nicht verfügbar.']};
export default function AdminJhccFinancePanel(){const locale=useAdminLocale(),t=copy[locale]||copy.en,[prepared,setPrepared]=useState<any>(null),[status,setStatus]=useState<any>(null),[confirm,setConfirm]=useState(false),[busy,setBusy]=useState(false);const prepare=async()=>{setBusy(true);setStatus(null);try{setPrepared((await httpsCallable(functions,'prepareAdminJhccFinanceReportV1')({})).data)}catch{setStatus({delivered:false,status:'unavailable',reason:'Authoritative finance evidence is unavailable.'})}finally{setBusy(false)}};const send=async()=>{if(!prepared||!confirm)return;setBusy(true);try{setStatus((await httpsCallable(functions,'transmitAdminJhccFinanceReportV1')({reportId:prepared.reportId,expectedReportDigest:prepared.reportDigest,commandId:`finance_${crypto.randomUUID()}`})).data)}catch{setStatus({delivered:false,status:'error',reason:'Transmission failed without a success receipt.'})}finally{setBusy(false);setConfirm(false)}};return <section className="transmission" aria-labelledby="jhcc-finance-title"><h3 id="jhcc-finance-title">{t[0]}</h3><p>{t[4]}</p><button disabled={busy} onClick={()=>void prepare()}>{t[1]}</button>{prepared&&<><dl><div><dt>Schema</dt><dd>{prepared.schema}</dd></div><div><dt>Report</dt><dd>{prepared.reportId}</dd></div><div><dt>EconomyConfig</dt><dd>{prepared.economyConfigVersion}</dd></div><div><dt>Evidence</dt><dd>{prepared.evidenceStatus}</dd></div></dl><label><input type="checkbox" checked={confirm} onChange={e=>setConfirm(e.target.checked)}/>{t[2]}</label><button disabled={busy||!confirm} onClick={()=>void send()}>{t[3]}</button></>}{status&&<p role={status.delivered?'status':'alert'} aria-live="polite">{status.status} · {status.receipt?.receiptId||status.reason}</p>}</section>}
