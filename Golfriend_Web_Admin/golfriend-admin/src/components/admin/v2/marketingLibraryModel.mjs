export const MARKETING_LOCALES=Object.freeze(['en','th','ko','ja','zh','es','fr','de']);export const MARKETING_CATEGORIES=Object.freeze(['app_screenshot','app_store_asset','course_letter','partner_letter','website_image','website_copy','just_golfriend_campaign','logo','advertising','oem_asset']);export const MARKETING_STATUSES=Object.freeze(['draft','review','approved','archived']);const dateOf=value=>{const millis=value?.toMillis?.()??value?._seconds*1000;return Number.isFinite(millis)?new Date(millis).toISOString():typeof value==='string'?value:null};export function normalizeMarketingAsset(asset){return{id:String(asset.assetId||asset.id||''),assetId:String(asset.assetId||asset.id||''),title:String(asset.title||'Untitled asset'),category:MARKETING_CATEGORIES.includes(asset.category)?asset.category:'website_copy',page:String(asset.page||'general'),locales:Array.isArray(asset.locales)?asset.locales.filter(x=>MARKETING_LOCALES.includes(x)):[],status:MARKETING_STATUSES.includes(asset.state||asset.status)?asset.state||asset.status:'draft',state:MARKETING_STATUSES.includes(asset.state)?asset.state:'draft',version:String(asset.currentVersionId||'No file'),versionCount:Number(asset.versionCount||0),updatedAt:dateOf(asset.updatedAt),owner:'Server authority',channel:String(asset.page||'general'),provenance:'Firebase callable projection',approval:String(asset.state||'draft'),previewKind:'unsupported',available:Boolean(asset.currentVersionId),downloadApproved:Boolean(asset.currentVersionId),disclosures:[],requiredLaunch:false};}export const filterMarketingAssets=(assets,{query='',category='all',status='all',locale='all'}={})=>{const needle=query.trim().toLowerCase();return assets.filter(a=>(!needle||[a.title,a.page,a.category].some(v=>String(v).toLowerCase().includes(needle)))&&(category==='all'||a.category===category)&&(status==='all'||a.status===status)&&(locale==='all'||a.locales.includes(locale)));};export function marketingSummary(assets){const byStatus=Object.fromEntries(MARKETING_STATUSES.map(status=>[status,assets.filter(a=>a.status===status).length])),coverage=Object.fromEntries(MARKETING_LOCALES.map(locale=>[locale,assets.filter(a=>a.locales.includes(locale)).length]));return{total:assets.length,byStatus,coverage,recent:[...assets].filter(a=>a.updatedAt).sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt))).slice(0,4)};}export const localeCoverageComplete=(assets,category)=>MARKETING_LOCALES.every(locale=>assets.some(asset=>asset.category===category&&asset.locales.includes(locale)));
