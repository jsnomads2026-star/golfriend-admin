const finite=value=>typeof value==='number'&&Number.isFinite(value)?value:null;
const location=row=>row&&typeof row.location==='object'&&!Array.isArray(row.location)?row.location:{};
const radians=value=>value*Math.PI/180;
export function haversineKm(origin,latitude,longitude){if(!origin||!Number.isFinite(origin.latitude)||!Number.isFinite(origin.longitude)||!Number.isFinite(latitude)||!Number.isFinite(longitude))return null;const dLat=radians(latitude-origin.latitude),dLng=radians(longitude-origin.longitude),a=Math.sin(dLat/2)**2+Math.cos(radians(origin.latitude))*Math.cos(radians(latitude))*Math.sin(dLng/2)**2;return 6371*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));}
/** One member-facing destination per canonical clubhouse. Course rows are only layout relationships. */
export function normalizeClubhouseDestinations(clubhouseRows,courseRows,origin=null,radiusKm=null){const layoutsByClubhouse=new Map();for(const course of Array.isArray(courseRows)?courseRows:[]){const key=String(course?.clubhouseId||course?.providerClubId||'').trim();if(!key)continue;(layoutsByClubhouse.get(key)||layoutsByClubhouse.set(key,[]).get(key)).push(String(course.providerCourseId||course.courseID||course.id||'').trim());}return (Array.isArray(clubhouseRows)?clubhouseRows:[]).map(row=>{const loc=location(row),id=String(row?.providerClubId||row?.clubhouseId||row?.id||'').trim(),latitude=finite(loc.latitude??row?.latitude),longitude=finite(loc.longitude??row?.longitude),distanceKm=haversineKm(origin,latitude,longitude);return {id,clubhouseId:id,providerClubId:id,displayName:String(row?.displayName||''),address:String(loc.address||''),city:String(loc.city||''),state:String(loc.state||''),country:String(row?.country||loc.country||''),latitude,longitude,distanceKm,withinRequestedRadius:radiusKm===null||distanceKm===null?null:distanceKm<=radiusKm,layoutIds:[...new Set(layoutsByClubhouse.get(id)||[])].filter(Boolean).sort()};}).filter(row=>row.id&&row.displayName).sort((a,b)=>a.displayName.localeCompare(b.displayName)||a.id.localeCompare(b.id));}
