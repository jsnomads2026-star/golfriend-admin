'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),launch=require('./launchMarkets');
test('launch market definitions include disabled Korea and Singapore',()=>{for(const country of ['KOREA','SINGAPORE'])assert.deepEqual(launch.disabledLaunchJobs.find(x=>x.country===country),{country,state:'disabled',providerCalls:0,courseWrites:0,requiresImmutableActivationReceipt:true});});
test('operations projection exposes real receipts queue changes and review exceptions', {skip:'LINEAGE GAP: asserts /golf_api_record_quarantine/ inside ../functions-course-read/index.js. That codebase differs between lineages — the symbol exists in functions-course-catalogue here, not in course-read. Not a defect in any imported function. Owner: functions-course-read re-baseline packet.'},()=>{const source=fs.readFileSync(path.join(__dirname,'../functions-course-read/index.js'),'utf8');for(const value of ['getCourseOperationsProjection','course_country_ingestion_receipts','course_acquisition_requests','golf_api_record_quarantine','providerFetchedAt','weightedCompleted','emergencyReserve','unavailable'])assert.match(source,new RegExp(value));});
