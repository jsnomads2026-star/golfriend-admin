import assert from"node:assert/strict";import{validateEnterpriseBookingProducer}from"./enterpriseBookingProducerRegistry.js";let n=0;const t=(x:string,f:()=>void)=>{f();console.log(`ok ${++n} - ${x}`)},now=1_800_000_000_000,raw={schema:"golfriend.enterprise-booking-correlation-producer.v1",producerId:`ebcp_${"a".repeat(24)}`,authenticatedUid:"service_uid",allowedContracts:["golfriend.enterprise-booking-correlation-intake.v1"],organizationIds:["org_1"],courseIds:["course_1"],hmacKeyVersion:2,status:"active",effectiveAt:new Date(now-1000).toISOString(),expiresAt:new Date(now+60000).toISOString(),version:3},input={authenticatedUid:"service_uid",claimProducerId:raw.producerId,contract:raw.allowedContracts[0],courseId:"course_1",organizationId:"org_1",nowMs:now};
t("active exact producer is accepted",()=>assert.equal(validateEnterpriseBookingProducer(raw,input).hmacKeyVersion,2));
t("revoked expired future wrong identity scope contract and key fail closed",()=>{for(const changed of[{...raw,status:"revoked"},{...raw,expiresAt:new Date(now-1).toISOString()},{...raw,effectiveAt:new Date(now+1).toISOString()},{...raw,authenticatedUid:"other"},{...raw,courseIds:["course_2"]},{...raw,allowedContracts:["other"]},{...raw,hmacKeyVersion:9}])assert.throws(()=>validateEnterpriseBookingProducer(changed,input),/UNAVAILABLE/)});
t("unknown fields and duplicate scope fail closed",()=>{assert.throws(()=>validateEnterpriseBookingProducer({...raw,secret:"x"},input),/UNAVAILABLE/);assert.throws(()=>validateEnterpriseBookingProducer({...raw,courseIds:["course_1","course_1"]},input),/UNAVAILABLE/)});console.log(`enterprise booking producer registry: ${n} checks passed.`);
