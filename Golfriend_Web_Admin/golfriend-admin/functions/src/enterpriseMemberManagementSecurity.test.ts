import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{resolve}from"node:path";import test from"node:test";const runtime=readFileSync(resolve(process.cwd(),"src/enterpriseMemberManagementRuntime.ts"),"utf8"),rules=readFileSync(resolve(process.cwd(),"../enterprise-authority.firestore.rules"),"utf8");
test("Auth App Check GF004 and verified representative are mandatory",()=>{assert.match(runtime,/enforceAppCheck:true/g);assert.match(runtime,/resolveEnterpriseCourseAuthority\(uid,scope\.membershipId,scope\.organizationId,scope\.propertyId,scope\.courseId,"member_management"\)/);assert.match(runtime,/a\.actorMembershipId!==scope\.membershipId/);assert.match(runtime,/agreementAcceptanceReceiptId/)});
test("only exact course member scope is searched",()=>{assert.match(runtime,/enterprise_course_member_scopes/);assert.doesNotMatch(runtime,/collection\("users"\)|enterprise_verified_golfers/);assert.match(runtime,/x\.organizationId!==s\.scope\.organizationId\|\|x\.propertyId!==s\.scope\.propertyId\|\|x\.courseId!==s\.scope\.courseId/)});
test("trial or active entitlement reuses onboarding authority",()=>{assert.match(runtime,/enterprise_trials/);assert.match(runtime,/a\.status===\"active_partner\"/);assert.match(runtime,/activationReceiptId/);assert.match(runtime,/a\.status===\"suspended\"/)});
test("provider and Auth side effects are absent",()=>{assert.doesNotMatch(runtime,/auth\(\)\.createUser|sendEmail|sendSms|sendPush/);assert.match(runtime,/sent:false/);assert.match(runtime,/createsAuthAccounts:false/)});
test("commands and receipts are immutable and replay safe",()=>{assert.match(runtime,/COMMAND_REPLAY_CONFLICT/g);assert.match(runtime,/enterprise_member_management_receipts/);assert.match(runtime,/immutable:true/g)});
test("all backend collections deny direct clients",()=>{for(const c of["enterprise_course_member_scopes","enterprise_member_management_commands","enterprise_member_management_receipts","enterprise_member_import_previews"])assert.ok(rules.includes(`match /${c}/{document=**} { allow read, write: if false; }`))});
