import assert from"node:assert/strict";import{BookingScopeError,derivePartnerBookingScope}from"./partnerBookingScope.js";let n=0;const test=(name:string,run:()=>void)=>{run();console.log(`ok ${++n} - ${name}`)},decision=(overrides:any={})=>({membershipId:"mem_1",organizationId:"org_1",propertyId:"prop_1",courseId:"course_1",role:"booking_staff" as const,mode:"read" as const,canMutate:true,sourceVersion:"source_1",...overrides});
test("exact grants become bounded delegated course proof",()=>{const x=derivePartnerBookingScope([decision(),decision({courseId:"course_2"})],Date.parse("2026-08-15T00:00:00Z"));assert.deepEqual(x.courseIds,["course_1","course_2"]);assert.deepEqual(x.delegatedCourseIds,x.courseIds);assert.equal(x.expiresAt,"2026-08-15T00:05:00.000Z")});
for(const changed of[{organizationId:"org_2"},{membershipId:"mem_2"},{role:"organization_admin"},{sourceVersion:"source_2"}])test("mixed authority fails closed",()=>assert.throws(()=>derivePartnerBookingScope([decision(),decision(changed)]),(e:any)=>e instanceof BookingScopeError&&e.code==="BOOKING_AUTHORITY_AMBIGUOUS"));
test("missing authority fails closed",()=>assert.throws(()=>derivePartnerBookingScope([]),/UNAVAILABLE/));test("source is stable",()=>assert.equal(derivePartnerBookingScope([decision()],1).sourceVersion,derivePartnerBookingScope([decision()],1).sourceVersion));console.log(`partner booking scope: ${n} checks passed.`);
